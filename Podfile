# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Master Detail with Header Child ViewController' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Master Detail with Header Child ViewController
  pod 'SnapKit', '~> 4.0.0'
  pod 'SDWebImage', '~> 4.0'

end
